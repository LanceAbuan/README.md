/** Default duration (seconds) for reveal animations. */
export const REVEAL_DURATION = 0.6;

/** Default Y offset (px) for fade-up reveal animations. */
export const REVEAL_Y_OFFSET = 30;

/** Delay (seconds) between staggered child elements. */
export const STAGGER_DELAY = 0.1;

/** Delay step (seconds) per item in staggered lists (skills, projects, etc.). */
export const STAGGER_STEP = 0.08;

/** How far (px) from the viewport edge to trigger the reveal. */
export const REVEAL_MARGIN = "-100px";

/** How far (px) from the viewport edge for project cards. */
export const PROJECT_REVEAL_MARGIN = "-50px";

/** Duration (seconds) for the scroll-arrow fade-in. */
export const SCROLL_ARROW_DURATION = 0.4;

/** Delay (seconds) for the scroll-arrow fade-in. */
export const SCROLL_ARROW_DELAY = 0.5;

/** How long (ms) the success/error states stay visible before resetting. */
export const FORM_STATUS_TIMEOUT_MS = 4000;

/** Navbar height (px) used for scroll-hide threshold. */
export const NAVBAR_SCROLL_THRESHOLD = 10;

/** Delay (ms) after scrolling stops before showing/hiding the navbar. */
export const NAVBAR_HIDE_DELAY_MS = 200;

/** Maximum height (px) of the mobile menu. */
export const MOBILE_MENU_MAX_HEIGHT = 400;

/** Delay (seconds) for the mobile menu backdrop fade. */
export const MOBILE_MENU_BACKDROP_DELAY = 0.1;
